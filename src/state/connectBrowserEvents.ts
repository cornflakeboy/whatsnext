import { Store } from './store';


/**  */
export const connectBrowserEvents = (store: Store) => {
  // foo.on('bar', () => {
  //   store.dispatch({
  //      type: 'lulu'
  //   });
  // })

  return store;
};
